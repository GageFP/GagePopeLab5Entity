@model IEnumerable<GagePopeLab5.Models.Weapon>

@{
    ViewData["Title"] = "Index";
}
<link rel="stylesheet" href="~/css/site.css" type="text/css" />
<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Damage
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.weaponName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.weaponDamage)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.weaponName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.weaponName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.weaponName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
